{
  "name": "compile-sass",
  "version": "1.0.0",
  "description": "Webpack environment ready to compile sass.",
  "main": "index.js",
  "author": "Arian Vega",
  "license": "GPL-3.0-only",
  "scripts": {
    "dev": "rimraf dist/ && webpack --config webpack/dev.js && rimraf dist/js",
    "prod": "rimraf dist/*.min.css && webpack --config webpack/prod.js && rimraf dist/js",
    "clean": "rimraf dist && rimraf src/dist"
  },
  "devDependencies": {
    "css-loader": "^5.2.6",
    "mini-css-extract-plugin": "^1.6.0",
    "postcss-loader": "^6.1.0",
    "postcss-preset-env": "^6.7.0",
    "rimraf": "^3.0.2",
    "sass": "^1.35.1",
    "sass-loader": "^12.1.0",
    "webpack": "^5.39.0",
    "webpack-cli": "^4.7.2"
  }
}
